# Webhooks deploy values — RENDERED by ../Deploy-Webhooks.ps1 into stage/app/ from
# deploy.config.json, then shipped to the server alongside deploy.sh. This is the ONLY
# bridge between the PowerShell config and the (static) bash deploy script. Do not
# edit rendered copies; change deploy.config.json.
NODE_MAJOR='__NODE_MAJOR__'
RUN_USER='__RUN_USER__'
APP_DIR='__APP_DIR__'
AUDIT_DIR='__AUDIT_DIR__'
