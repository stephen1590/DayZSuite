# GameServices - CLAUDE.md changelog

Rationale for the rules in [CLAUDE.md](CLAUDE.md). History only - read when auditing or pruning a rule, not loaded into every request. Keep dated entries here, never back in CLAUDE.md.

Split out of CLAUDE.md on 2026-07-24 (owner directive: no changelogs in CLAUDE.md - it loads verbatim into every request).

---

### 2026-07-29

- **Executed** Phase 3 (part 1, 2026-07-29): adopted the KEEP-the-niche rump decision from the owner's own scoping statement (surfaced as a veto-able assumption), and completed the kept engine - `Set-XmlNode` in Apply-ConfigOverrides now uses a SelectNodes loop (TDD: tests/apply-overrides-multimatch.test.ps1 written first, seen failing on the every-node assertion, 4/4 after; gate 24/0 - single-match selectors byte-identical behavior, multi-match reported per-node). Closes the SelectSingleNode Open Work item at BUILT+GATED; LIVE ships with the next DayZ deploy. Queued Phase 3 part 2: retire override-diff*.ts + the whole-doc-edit-derives-delta UI path for patch rows (the 2026-07-20 doc's named root-cause drift machine), now redundant under the two-copy editor.
- **Consolidated** post-sweep (2026-07-29): `Pull-Configs -Execute` mirrored the slim box doc down (repo mirror now 175 leaves) + pulled the pending live-row drift (AIPatrolSettings/SpawnSettings mirrors, both tuning files, 2 defaults) with the ONE authorized auto-commit (552b29b). Offline gate re-run against the fresh slim mirrors: 24/0, zero-MISS holds - the whole post-migration chain is coherent. CONFIG-ARCHITECTURE.md status/target headers updated to the built reality. BLOCKED on owner: (1) browser pass of the CM6 editor, (2) Phase 3 rump decision.
- **Completed** config-migration **Phase 2** (2026-07-29): all 15 chaotic targets cut over on prod - 12 remaining Loadouts + PlayerMaleSuit (owned-by-classification even at 20 leaves - consistency over threshold), AirdropSettings, BookSettings, + the empty SpawnerBubaku block. Targets ENUMERATED BY SEARCH from the live doc, one guarded override-write per file (base= chained), a snapshot per step, and `sha256sum -c` proof that ALL live files stayed byte-identical. AirdropSettings tripped the shrink guard on the final cut (its 4,009 leaves exceeded half the by-then-shrunken doc) - the guard doing its designed job on a mass-delete; re-sent with `force` + the same structural/live-file proofs. Box overrides doc: 14,881 -> 175 leaves (-98.8%); the remaining rump is the genuine-field-tweak class + parked disabled-mod patches, held for the Phase 3 decision. Pre-sweep browser risk was bounded by: served import graph fully resolving (14/14 specifiers 200), parse-clean modules, byte-exact assets, the signed API chain - the headless firefox screenshot itself failed (likely desktop profile lock; stated, not hidden). Repo mirror still holds the fat doc until the next prod pull - do NOT hand-edit it (sync hash-guard).
- **Tested prod non-destructively, then executed on pass** (owner directive 2026-07-29). Tests, all zero-net-change: dayz-ctl round-trip own-write on Traders/Aircraft.json (rc=0, file byte-identical 4937f68e…, snapshot created in .own-versions/); FULL SIGNED API chain on-box with a real full-scope key (nothing printed): configs/owned 200 = 14F+4D, configs/own 200, configs/set-own byte-identical round-trip 200 with version unchanged, stale baseVersion -> 409, reference path -> 404; served assets (own-editor.js, editor.js, cm6.esm.js, style.css) sha-match local with correct MIME. Unsigned-401 probing was INCONCLUSIVE (auth gate fires before routing) - the signed chain is the proof. FOUND + FIXED during testing: OWNED_FILES included the web:'types' tuning pair, letting own-write bypass types-write's structural CE validation - Deploy-Api now excludes types rows (12F+4D), redeployed, live-verified (own-write on the tuning file -> exit 2). **Phase 2 cutover #1 DONE: TownLoadout** - 553 leaves removed from the box overrides doc (structural proof: rest identical), guarded override-write base=f88d146b -> new doc cacb75df, live file byte-identical (663c446f…), rollback snapshot 20260729T233038Z, own-read serves it; the row now auto-opens the whole-file editor. 13 targets remain; sweeping the other 12 Loadouts waits on the owner's browser pass of the editor against TownLoadout.
- **Deployed** config-migration **Phase 1 to PROD** (Api + ConfigViewer, 2026-07-29 ~23:22Z; owner go = "continue" on the stated deploy offer). Api: dayz-ctl re-rendered with own-read/own-write/config-owned + OWNED masks, actions built + service restarted clean. ConfigViewer delta was ONLY the Phase 1 files (style.css, editor.js, own-editor.js, vendor/codemirror/cm6.esm.js) - the parallel session's chat/logs work was already deployed on its side, nothing rode along. Box-verified non-mutating: `config-owned` emits 14F+4D exactly as rendered; `own-read` serves Traders/Aircraft.json (sha+raw) AND both halves of the TownLoadout two-copy pair (live + .defaults companion); stale `base=` -> exit 5 with the file byte-identical; a reference path -> exit 2. Remaining unchecked box (owner): browser pass of the own-editor on a zero-patch owned file, then Phase 2 cutovers begin (one Loadout first).
- **Built** the config-migration **Phase 1 FRONT-END** (not deployed): `ConfigViewer/web/js/own-editor.js` - the two-copy whole-file editor on the vendored CM6 (dynamic import, so the 445KB bundle loads on first use only; `unifiedMergeView` shows the diff vs the frozen `<stem>.defaults` companion, read via the SAME `configs/own` action since it sits under the owned dir - display-only, the box never applies diffs; JSON/XML syntax by extension; save = whole doc via `configs/set-own` with baseVersion -> 409 reload flow; drafts survive file switches like the other editors). `editor.js` wired (9 targeted edits): fetches `configs/owned` fail-soft (old API = nothing routes, zero behavior change until deploy), and routes a row to the own-editor ONLY when it is owned AND carries ZERO active override patches - a patched file keeps the override editor until its Phase 2 cutover empties the block, then flips automatically; that flip IS the migration UX. Editing bytes (not parsed values) sidesteps the lossless-json round-trip for migrated surfaces. Proofs: `node --check` on both modules; DOM-free `EditorState` smoke composing the exact extension stack (basicSetup + lang + listener + unifiedMergeView) for json AND xml. LIVE unchecked: needs the Api deploy (verbs/masks/actions) + ConfigViewer deploy (owner go for prod), then a browser pass on a zero-patch owned file (a Traders file is the natural first check).
- **Built** the config-migration **Phase 1 BACKEND** (not deployed): generic owned-file verbs `own-read`/`own-write` in dayz-ctl (TDD - `Api/deploy/tests/own-verbs.test.sh` written FIRST, seen failing 8/16 on "unknown verb", then 16/16: sha+raw read, replace + new-sha write, snapshot to `.own-versions/` keep-30, `base=` conflict exit 5, JSON/XML parse-before-write, refuses non-owned/generated/disabled-mod/traversal/.env/absent), gated by `OWNED_FILES`/`OWNED_DIRS` masks Deploy-Api renders from registry `category:'owned'` rows (14 files + 4 folders against the real registry); `config-owned` enumeration verb -> `configs/owned` action so the editor can route rows; `configs/own`/`configs/set-own` actions mirror the types pair (409 on conflict, 2MB cap). Also fixed the stale allowed-verbs die list (config-disabled + settings-* were missing - live prose-drift specimen). CodeMirror 6 vendored for the two-copy editor: `ConfigViewer/vendor-build/` (npm+esbuild harness, node_modules gitignored) -> `web/vendor/codemirror/cm6.esm.js` (445KB ESM, minified), node smoke test proves all 10 needed exports (EditorView/State/Compartment/basicSetup/json/xml/MergeView/unifiedMergeView/keymap/lineNumbers). Deploy rsyncs `web/` only, so the harness never ships. The bespoke types-/settings- writers stay until their surfaces migrate onto the generic verb. REMAINING Phase 1 (ledgered): `own-editor.js` (unifiedMergeView; the default copy is the `.defaults` companion, reachable via `configs/own` since it sits under the owned dir), editor.js routing on `configs/owned`, CSS, then the Api deploy.

- **Activated** the config-overrides deprecation (CONFIG-ARCHITECTURE.md target model) as owner priority, and executed **Phase 0**: doc re-baselined to verified current reality (override doc ~98KB -> 1.1MB / 14,881 leaves; stale prestart engine list corrected - AI draft builders + Babaku retired 07-23/24; types pair + AIPatrolSettings + bans/whitelist noted as already-migrated prototypes), registry rows gained `category` (owned | computed | reference; the `overrides` row deliberately untagged - it IS the retiring engine), and the migration worklist recorded (14 whole-doc-rewrite targets = 98.7% of leaves -> Phase 2; 19 genuine field tweaks = 1.3% -> Phase 3 decision; dead weight incl. an EMPTY SpawnerBubaku block). Owner decisions logged: Loadouts are OURS (should be owned files, never override targets); the whole-file editor needs XML syntax support (CodeMirror 6 covers it); the stale 2026-07-20 config-work HOLD treated as superseded (assumption surfaced - if the duplication investigation is still live, Phase 1 pauses). No consumer reads `category` yet - proven non-breaking by the offline gate.

### 2026-07-24
- **Rebuilt** the ConfigViewer Map tab's AI editor. The Phase 5 patrol editor had rendered its form into the left sidebar (`mapNav`, directly above Sign out); it now renders IN the detail panel (`mapDetail`) in place, reusing the existing `.me-*` inline-edit system (`mapEditFormHtml` pattern) rather than the parallel `.pe-*` sidebar form it had reinvented. Covers patrols, object patrols, AND global settings; Cancel restores the readout with the Edit button intact (no reload). Retired the sidebar `renderPatrolEdit` + `.pe-*` styles. Also added, in one pass: an **Inline/Stacked** field-density toggle (persisted; inline labels truncate in a 64px column, full name on hover); the raw **Waypoints listed + editable** in the panel (X/Z inputs, delete, Y preserved) instead of "drawn on the map only"; **kind glyphs** in the nav list (reusing `spawnClassSvg` — location ring / patrol hexagon / object diamond); a **sticky quick-filter** and **A–Z** sort; **grouping by type** (Patrols / Object patrols / Locations — collapsible, patrols open by default, Grouped⇄A–Z toggle) as a **list-only** view — the nav grouping/collapse never touches the map; map visibility stays owned solely by the map-rail category filter (one control for that output, not two); **resizable panels** — draggable seams on the sidebar (grid column), detail panel, and tools rail (flex-basis; widths persist in localStorage; a `ResizeObserver` on `.map-stage` re-fits the canvas; CSS-disabled on the <760px single-column layout); and the **LoadBalancingCategories editor** in global settings — a collapsible tier table per category (0-indexed rows of `{MinPlayers, MaxPlayers, MaxPatrols}`), add/rename/delete category + add/remove tier, an "N used" chip, rename moves patrol references along, delete warns when in use. A patrol's `LoadBalancingCategory` became a soft-linked datalist (pick a defined category or free-type; **not enforced** — Expansion falls back to Global). The editor is DATA-DRIVEN: it renders whatever keys the live `AIPatrolSettings.json` entry has; only `PATROL_CORE` (which fields are "core" vs Advanced) and the LBC tier shape are hardcoded, at the UI level — there is no field schema (a registry-level schema is a deferred option if validation is ever wanted). Global settings stays IN-PANEL; linking it to the main Config editor is a deferred consolidation (the file is `mapOwned`-locked there, and that view only offers a raw whole-file textarea anyway). Shipped via the ConfigViewer static rsync (map.js + style.css + index.html), sha-verified on the box; no Api or DayZ deploy involved.

### 2026-07-23
- **Added** per-layer map calibration to the ConfigViewer Map tab. ROOT CAUSE it fixes: the renderer placed EVERY tile layer onto the same world rectangle `[0,worldSize]` with one shared transform, so a layer aligned only if its source image already spanned that rectangle - true for the heightmap-derived `terrain` layer (aligned by construction), false for the higher-res rips (`Sat (New)`, `Topo (New)`, `satellite`) whose pixel bounding box differs. There was no per-layer correction, and the old "2-point sync" was a one-time manual nudge, never persisted. Now each layer carries an optional AFFINE (`map-calibration.json`, a COMMITTED sidecar - the manifest is gitignored + regenerated by MapDataExtraction, so calibration can't live there) that maps assumed-world → true-world; the renderer honors it (`drawTileAffine`, inverse-mapped tile culling) and markers stay in true world coords. **Absent/identity calib = the literal pre-existing draw path**, so aligned layers (Topo Old + everything working) cannot regress - proven offline: the calibrated path at identity produces byte-identical tile placement. Author via a **Calibrate mode** (`js/map-calibrate.js` = pure, unit-proven solver; UI in map.js): mark control points (click the landmark on the raw layer, type its true X/Z read off the accurate layer), 2 pts → offset+scale, 3+ → full affine via least-squares, live residual/RMS/rotation readout, Preview toggle, Copy emits the sidecar block. Answer to "how many points": 2 minimum (offset+scale), 3 for rotation, 3-5 spread for robustness - the residual readout tells you if the simpler model sufficed. The solver, the affine inverse, the identity-preservation, and a full cropped+scaled+rotated round-trip are all proven offline (no browser needed for the math); the DOM/canvas interaction is verified by USING it (non-destructive - preview only until you Copy + deploy). Ships with the ConfigViewer deploy (static assets, no Api/DayZ involvement).

### 2026-07-22
- **Added** the StarDZ DayZ Modding Wiki as a git submodule (`StaticishSite/external/DayZ-Modding-Wiki`, pinned `1148527`), mounted into the static site one node BELOW our own docs at `/docs/reference/dayz-modding-wiki/`. Chosen over embedding it in ConfigViewer's Mod Docs (which would have taken that tab from 6 mod readmes to 111 pages) and over a top-level site section (which would bury 6 of our own pages under 105 of someone else's). **Only `en/` is mounted** - the repo carries 12 languages and 44 MB. The submodule is a BUILD-time dependency only: `Deploy-Site` ships `./public`, so the box never sees the source. Second submodule in this repo after `themes/hugo-book`, so `git clone --recursive` was already required.
- **Added** `layouts/_default/_markup/render-link.html`. The wiki cross-references itself with ~1,200 relative `.md` links and Hugo publishes each page as its own directory, so they need both the `.md` stripped AND one extra `../` (the page sits one level deeper than its source file). Two bugs found by actually sweeping the rendered HTML rather than eyeballing it: missing the extra `../` left **948 of 13,804** links dead, and lower-casing paths left another **149** dead because this site sets `disablePathToLower` (Hugo publishes `README/`, not `readme/`). The hook also sends the upstream's 11 sibling-language links and `CONTRIBUTING.md` to GitHub, since only `en/` is mirrored. Final sweep: **0 broken links**.
- **Note** on licence: the wiki is CC BY-SA 4.0 and we now REPUBLISH it on the public apex domain. `content/docs/reference/dayz-modding-wiki/_index.md` carries the attribution, licence link, pinned commit and an accuracy caveat (its serverDZ.cfg chapter documents 25 parameters and omits several real ones). Keep that page if the mirror stays - it is the attribution requirement, not decoration.
- **Added** an "Other links" group at the bottom of the ConfigViewer's Mod Docs tab, driven by `ConfigViewer/web/mod-links.json`. Everything else in that tab is a doc file read off the box out of an `@mod` folder; these are the off-box SOURCES those mods come from, so "where is this documented?" is answered next to the docs instead of in someone's bookmarks. Seven links: Expansion Scripts + Missions, VPP repo + wiki, the StarDZ community wiki, and dzconfig's serverDZ.cfg page + wiki index. Static asset fetched same-origin with no credentials - **ConfigViewer deploy only, no Api or DayZ redeploy**. Adding a link = one row in that JSON. The renderer drops anything whose URL is not an absolute http/https one (parsed with NO base: resolving against `location.href` would turn a typo like `github.com/foo`, or any junk string, into a same-origin link pointing at our own site), and the links render even when the box has no mod docs at all - that is exactly when they matter most.

### 2026-07-21
- **Consolidated** into ONE GameServices repo: former `DayZ-Server` repo re-rooted with the game server under `DayZ-Server/`, former `NginxService` repo grafted in via `git subtree add` and flattened to the root (no wrapper folder), `staging/` + the planning MDs moved in. Both git lineages preserved (two root commits reachable from HEAD). Reason: everything here deploys together and shares the DayZ dependency; the repo boundary was structure without meaning. Only two path patches were needed: Api's registry ref (`../../../DayZ-Server` → `../../DayZ-Server`) and staging's Utils depth.
- **Moved** the CryptPad service out to its own independent repo `../CryptPad/` (history carried via `git subtree split`) - no DayZ relationship. The engine gained generic sibling-resolution + `-HostConfigDir` so CryptPad provisions from here without copying anything.
- **Fixed** CryptPad's `deploy.config.json` - was a truncated copy missing the closing brace AND the admin signing key; the lock-out guard would have refused every deploy. Restored; the unreferenced `.cytonicmushroom.json` duplicate retired.
- **Fixed** all six ssh call sites to omit `-p` when `SshPort` is unset (was hardcoding 22, which overrode the `staging-vm` alias and pointed deploys at the dev machine's own sshd).
- **Added** the `-Env` selector (staging default, prod explicit) across all deploy scripts + Provision-Tls - STAGING-PLAN.md phase 1.
- **Added** staging-by-default deploy rule; local QEMU staging VM (`staging-vm`, Ubuntu 26.04) for stakeholder review before prod.

### 2026-07-20
- **Added** Config architecture doctrine after a 4-lane reassessment found the config model re-implemented across browser/API/box. Durable design record: `CONFIG-ARCHITECTURE.md`.

### 2026-07-17
- **Added** Grafana to `Monitoring/` - OSS package, loopback :3001, dashboards-as-code, 30d retention (owner call).
- **Added** `Monitoring/` (Prometheus + node_exporter) + the Api `GET /metrics` exporter.
- **Added** `common/Deploy-Helpers.ps1` - first slice of the P1 deploy-layer refactor.
- **Created** the predecessor CLAUDE.md files - repackaged rules out of session memory.
